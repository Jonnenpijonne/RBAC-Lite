# RBAC-Lite Safety Fix Tag Note

Suggested release tag for the safety fix:

```text
v0.1.1-rbac-lite-safety-fix
```

Target commit:

```text
eaf4d5cab2ae6f8454d42bec6e57d2e40ee1f7a3
```

Local command:

```bash
git fetch origin
git switch main
git pull origin main
git tag -a v0.1.1-rbac-lite-safety-fix eaf4d5cab2ae6f8454d42bec6e57d2e40ee1f7a3 -m "RBAC-Lite safety fix"
git push origin v0.1.1-rbac-lite-safety-fix
```
